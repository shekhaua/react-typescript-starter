{
  "name": "api",
  "version": "1.0.0",
  "description": "React Redux App API.",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --delay 1500",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.0"
  }
}
